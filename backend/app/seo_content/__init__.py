"""SEO content modules — guides + intent pages.

Editorial content (long-form guides, intent landing pages) lives as pure
Python data so the same source powers both the server-rendered HTML and
any future JSON API consumed by the React app.
"""
